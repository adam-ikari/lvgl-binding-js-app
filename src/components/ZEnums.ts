enum ZWidthEnum {
  Auto = "auto",
  Full = "full",
}

enum ZHeightEnum {
  Auto = "auto",
  Full = "full",
}

enum ZSizeEnum {
  Small = "small",
  Default = "default",
  Large = "large",
}

export { ZWidthEnum, ZHeightEnum, ZSizeEnum };
